services:
  stockfish:
    build:
      context: ./stockfish
      dockerfile: Dockerfile
    container_name: stockfish
    ports:
      - "4001:4001"
    networks:
      - engine
      
  hyper:
    build:
      context: ./hyper
      dockerfile: Dockerfile
    container_name: hyper
    ports:
      - "4000:4000"
    depends_on:
      - stockfish
    networks:
      - engine

networks:
  engine:
    driver: bridge